const initialState = {
    scope: 'SIE users',
    userStatus: 'disconnected',
    userInfo: {},
    allUsers: {},
}

const reducer = (state = initialState, action: any) => {
 switch (action.type){
     default:
         return state
 }
}

export default reducer;